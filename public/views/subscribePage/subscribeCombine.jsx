import SubscribeDays from './subscribeDays';
import SubscribeLevel from '/subscribeLevel';
import SubscribeRecipient from './subscribeRecipient';

class SubscribeCombine extends Component {
    var fields = {}
    saveFields (newFields) {
        fields = Object.assign({}, fields, newFields);
    }
    render () {
        return ( null; );
    }
}
